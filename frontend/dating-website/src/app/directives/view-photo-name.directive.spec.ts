import { ViewPhotoNameDirective } from './view-photo-name.directive';

describe('ViewPhotoNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewPhotoNameDirective();
    expect(directive).toBeTruthy();
  });
});
